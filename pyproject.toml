[build-system]
requires = ["setuptools>=77.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "jamica"
version = "0.3.0"
description = "Native Python implementation of AMICA for MNE-Python and scientific EEG workflows."
readme = { file = "README.md", content-type = "text/markdown" }
license = "BSD-3-Clause"
license-files = ["LICENSE"]
requires-python = ">=3.10"
authors = [
  { name = "Sina Esmaeili", email = "sina.esmaeili@umontreal.ca" },
]
keywords = [
  "jamica",
  "amica",
  "ica",
  "eeg",
  "mne",
  "neuroscience",
  "blind-source-separation",
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Science/Research",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Scientific/Engineering",
  "Topic :: Scientific/Engineering :: Information Analysis",
]

dependencies = [
  "numpy>=1.21",
  "scipy>=1.7",
]

[project.optional-dependencies]
jax = [
  "jax>=0.4",
]

gpu = [
  "jax[cuda12]>=0.4",
]

mne = [
  "mne>=1.0",
]

# AmicaICA.save() writes HDF5 through MNE's h5io helper, the same one
# EOGRegression uses. Optional in MNE, so optional here too.
hdf5 = [
  "h5io",
]

icalabel = [
  "mne-icalabel>=0.4",
  "onnxruntime",
]

viz = [
  "matplotlib>=3.5",
]

test = [
  # The suite imports these, so they belong here rather than in the feature
  # extras alone. Without jax the tests silently exercise the NumPy fallback
  # instead of the backend being shipped, and tests/test_viz.py imports
  # matplotlib at module scope, so its absence is a collection error that stops
  # the whole run -- which is what `pip install -e ".[dev]"` followed by
  # `pytest`, exactly as CONTRIBUTING.md documents it, used to produce.
  "jamica[jax,viz]",
  "pytest>=8.0",
  "pytest-cov",
  "pytest-xdist",
  "pytest-timeout",
  "pandas>=2.0",
  "scikit-learn>=1.0",
  # Two memory tests in test_amica.py skip without it, quietly.
  "psutil",
  "python-picard",
  # The AmicaICA save/load tests need it; without it they skip and the
  # persistence path ships untested.
  "h5io",
]

docs = [
  "sphinx>=7.0",
  "numpydoc",
  "sphinx-gallery",
  "pydata-sphinx-theme",
  "myst-parser",
  "sphinx-copybutton",
  "sphinx-design",
  "towncrier",
  "linkify-it-py",
]

dev = [
  "jamica[test,docs]",
  "pre-commit>=3.0",
  "ruff>=0.9",
  "towncrier>=23.0",
  "nox",
  "build",
  "twine",
]

release = [
  "build",
  "twine",
  "towncrier>=23.0",
]

all = [
  "jamica[jax,mne,hdf5,icalabel,viz,test,docs]",
]

[project.urls]
Homepage = "https://github.com/snesmaeili/jamica"
Documentation = "https://snesmaeili.github.io/jamica/"
Repository = "https://github.com/snesmaeili/jamica.git"
Issues = "https://github.com/snesmaeili/jamica/issues"
Changelog = "https://github.com/snesmaeili/jamica/blob/main/CHANGELOG.md"

[tool.setuptools.packages.find]
include = ["jamica*"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = [
  "E",
  "F",
  "W",
  "I",
  "UP",
  "B",
  "C4",
  "SIM",
  "PIE",
  "RET",
  "RUF",
]
ignore = [
  "RET504",
]

[tool.ruff.lint.isort]
known-first-party = ["jamica"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
"docs/**" = ["E402", "F401"]
"examples/**" = ["E402"]
"tests/**" = ["S101"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"
docstring-code-format = true

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
log_level = "INFO"
addopts = "-ra"
markers = [
  "gpu: requires --backend=gpu and a reachable JAX GPU device",
  "slow: long-running; pass --run-slow to include",
  "timeout: per-test timeout in seconds (pytest-timeout)",
]
filterwarnings = [
  "error",
  "ignore:Unrecognized profile:UserWarning",
  "ignore:Unknown section Matrix Naming Convention:UserWarning",
  "ignore:Citation.*is not referenced:UserWarning",
  "ignore:The rcond argument for linalg.pinv is deprecated:DeprecationWarning",
]

[tool.coverage.run]
source = ["jamica"]
omit = [
  "jamica/binary.py",
]

[tool.towncrier]
package = "jamica"
filename = "CHANGELOG.md"
directory = "changes"
title_format = "## [{version}](https://github.com/snesmaeili/jamica/releases/tag/v{version}) ({project_date})"
issue_format = "[#{issue}](https://github.com/snesmaeili/jamica/pull/{issue})"
template = "changes/template.md"

[[tool.towncrier.type]]
directory = "enh"
name = "Enhancements"
showcontent = true

[[tool.towncrier.type]]
directory = "bugfix"
name = "Bug Fixes"
showcontent = true

[[tool.towncrier.type]]
directory = "api"
name = "API Changes"
showcontent = true

[[tool.towncrier.type]]
directory = "dep"
name = "Dependencies"
showcontent = true

[[tool.towncrier.type]]
directory = "doc"
name = "Documentation"
showcontent = true

[[tool.towncrier.type]]
directory = "misc"
name = "Miscellaneous"
showcontent = true
