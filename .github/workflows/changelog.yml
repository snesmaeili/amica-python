name: Changelog

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  check-changelog:
    name: Check changelog fragment
    runs-on: ubuntu-latest
    # Dependabot never writes a fragment, and a dependency bump does not need a
    # user-facing changelog entry, so requiring one only blocks the merge.
    if: github.actor != 'dependabot[bot]'

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip

      - name: Install towncrier
        run: |
          python -m pip install --upgrade pip
          python -m pip install towncrier

      - name: Check changelog fragment
        run: |
          towncrier check --compare-with origin/main
